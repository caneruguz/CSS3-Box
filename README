This project is a CSS3 property generator for boxes and includes border radius and shadow. It allows the user to change values in relevant fields and see immediate changes in the shape of the example box. The page also provides the exact CSS3 code the user can copy and paste to own css element.  